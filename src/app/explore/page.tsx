import { ExploreClient } from '@/components/ExploreClient';

export const metadata = {
  title: '탐구 — 사주 탐구',
  description: '만세력 좌표를 산출하고, 정답 없이 깊이 탐구합니다.',
};

export default function ExplorePage() {
  return <ExploreClient />;
}
