import { LandingClient } from '@/components/landing/LandingClient';

export const metadata = {
  title: '8-bit',
  description: '생년월일시로 만세력 좌표를 산출합니다. 원리 공개, 투명한 명리 탐색.',
};

export default function HomePage() {
  return <LandingClient />;
}
